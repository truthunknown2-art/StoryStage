# Pro technical acceptance — KCAST deterministic preparation

Date: 2026-07-19  
Verdict: **ACCEPTED**  
Accepted exact implementation SHA: `86cf1d89e27c352c9eb0e9e27a188a2899ebcaa5`  
Repository: `truthunknown2-art/StoryStage`  
Branch: `agent/kcast001-preparation-worker`  
Draft PR: `#6`

## Accepted checkpoint

Pro found no remaining P0/P1 blocker in deterministic character-rig view preparation. The previous topology-authority rejection is closed by the canonical, deeply frozen `kids-biped-v1` template and its exact content hash `6ab97203293ff1736587a224b515d9ce5562825442d02e8cd31b98c4db588c83`.

The accepted implementation enforces:

- the exact 29-part semantic set and one torso root;
- canonical torso, pelvis, head, shoulder/elbow/wrist, hip/knee/ankle, and head-local facial chains;
- exact semantic socket ownership with no left/right cross-wiring;
- all 13 legal exposure targets, including every viseme to `mouth-rest`;
- request binding to the exact canonical topology hash;
- role-based part matching, exact parent/socket validation, and no missing or extra child sockets;
- complete receipt, persisted-evidence, staged-byte, content-addressed output, and private manifest-sealing boundaries;
- adversarial rejection of torso-star topology, cross-wired limbs, and invalid exposure targets;
- deterministic byte-identical preparation for a valid articulated chain.

## Authorized next slice

Proceed with actual complete source acquisition and deterministic preparation of:

- `front`
- `profile-left`
- `profile-right`

The rig-family builder must reopen every view's complete evidence chain and call `validatePreparedCharacterRigViewManifest()` with the exact request, bundle, staging report, import receipt, recipe, and manifest. A standalone schema parse is not production authority.

No view becomes production-bindable until all three prepared views are assembled into the family manifest and the moving diagnostic receives Preston's explicit approval.

## Operational merge gate

Acceptance applies to exact implementation SHA `86cf1d89...`. PR merge still waits for hosted GitHub verification to finish green. Test-only commit `bebedec45be121444fb12c815b430b98eeab4931` changes only the single Sharp-heavy side-by-side-sheet test's per-test timeout from 5 seconds to 10 seconds after unchanged hosted runs crossed the prior limit at 5.016 and 5.010 seconds. It changes no production code, pixels, assertions, dimensions, PNG settings, or content hashes.
